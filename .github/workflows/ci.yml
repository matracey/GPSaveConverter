name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: windows-latest
    permissions:
      checks: write

    strategy:
      matrix:
        configuration: [Debug, Release]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.x'

      - name: Restore and Build
        run: dotnet build GPSaveConverter.sln --configuration ${{ matrix.configuration }} --verbosity minimal

      - name: Run tests
        run: dotnet test GPSaveConverter.Tests/GPSaveConverter.Tests.csproj --configuration ${{ matrix.configuration }} --verbosity normal --filter "Category!=Smoke" --logger "trx;LogFileName=test-results.trx"
        env:
          STEAM_API_KEY: ${{ secrets.STEAM_API_KEY }}

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results-${{ matrix.configuration }}
          overwrite: true
          path: GPSaveConverter.Tests/TestResults/

      - name: Publish unit test results
        if: always()
        uses: dorny/test-reporter@v3
        with:
          name: Test Results (${{ matrix.configuration }})
          path: GPSaveConverter.Tests/TestResults/*.trx
          reporter: dotnet-trx

      - name: Upload build artifacts
        if: matrix.configuration == 'Release'
        uses: actions/upload-artifact@v7
        with:
          name: GPSaveConverter-Release
          overwrite: true
          path: GPSaveConverter/bin/Release/

      - name: Upload installer artifact
        if: matrix.configuration == 'Release'
        uses: actions/upload-artifact@v7
        with:
          name: GPSaveConverter-Installer
          archive: false
          overwrite: true
          path: GPSaveConverter.Installer/bin/**/Release/**/*.msi

      - name: Upload bootstrapper artifact
        if: matrix.configuration == 'Release'
        uses: actions/upload-artifact@v7
        with:
          name: GPSaveConverter-Bootstrapper
          archive: false
          overwrite: true
          path: GPSaveConverter.Bundle/bin/**/Release/**/*.exe
